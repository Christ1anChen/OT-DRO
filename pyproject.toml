[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "OT-DRO"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "mosek"
]

[tool.setuptools.packages.find]
where = ["src"]